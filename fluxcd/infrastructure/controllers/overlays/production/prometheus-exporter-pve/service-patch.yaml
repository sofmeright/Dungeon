apiVersion: v1
kind: Service
metadata:
  name: prometheus-exporter-pve
  annotations:
    lbipam.cilium.io/ips: "172.22.30.137"
    lbipam.cilium.io/sharing-key: "gossip-stone"
    lbipam.cilium.io/sharing-cross-namespace: "*"
spec:
  type: LoadBalancer
  ports:
    - port: 9221
      targetPort: 9221
      protocol: TCP
      name: metrics
