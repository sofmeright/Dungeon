apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hyrule-castle

resources:
  - ../../../base/harbor
  - httproute.yaml
  - externalsecret.yaml
  - rgw-admin-secret.yaml
  - rgw-setup-job.yaml

patches:
  - path: postgres-cluster-patch.yaml
  - path: redis-patch.yaml
  - path: helmrelease-patch.yaml
