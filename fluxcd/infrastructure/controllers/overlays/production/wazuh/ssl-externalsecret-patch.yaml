apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: wazuh-ssl-certs
spec:
  secretStoreRef:
    name: vault-zeldas-letter
  dataFrom:
    - extract:
        key: apps/wazuh
