apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: loki
spec:
  targetNamespace: gossip-stone
  values:
    singleBinary:
      persistence:
        size: 30Gi
        storageClass: ceph-rbd-retain
