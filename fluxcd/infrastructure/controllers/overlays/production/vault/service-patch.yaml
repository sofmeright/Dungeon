apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: vault
spec:
  selector:
    app.kubernetes.io/name: vault
    vault_cr: vault
    # Remove vault-active selector that pod doesn't have