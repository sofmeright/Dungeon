apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gitlab-redis
spec:
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes:
      - ReadWriteOnce
      storageClassName: ceph-rbd-retain
      resources:
        requests:
          storage: 5Gi
