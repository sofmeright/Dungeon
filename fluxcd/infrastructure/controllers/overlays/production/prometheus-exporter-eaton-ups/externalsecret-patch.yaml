apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: prometheus-exporter-eaton-ups
spec:
  secretStoreRef:
    name: vault-zeldas-letter
  target:
    template:
      data:
        config.json: |
          {
            "5PX3000": {
              "address": "https://10.24.7.30",
              "user": "{{ .user }}",
              "password": "{{ .password }}"
            }
          }
  dataFrom:
    - extract:
        key: apps/prometheus-exporter-eaton-ups
