apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: prometheus-exporter-eaton-ups
spec:
  secretStoreRef:
    name: vault-operationtimecapsule
  dataFrom:
    - extract:
        key: infrastructure/prometheus-exporter-eaton-ups
