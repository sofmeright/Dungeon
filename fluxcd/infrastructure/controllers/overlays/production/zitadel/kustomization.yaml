apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/zitadel
# Production-specific patches can go here if needed
# patchesStrategicMerge:
#   - service-patch.yaml  # Example: different LoadBalancer IP for prod