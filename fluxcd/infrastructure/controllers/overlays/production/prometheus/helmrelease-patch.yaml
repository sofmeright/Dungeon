apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: prometheus
spec:
  targetNamespace: gossip-stone
  values:
    server:
      persistentVolume:
        size: 50Gi
        storageClass: ceph-rbd-retain

      service:
        type: ClusterIP
