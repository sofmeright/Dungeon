apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/cert-manager
patches:
  - target:
      kind: HelmRelease
    patch: |
      - op: replace
        path: /spec/targetNamespace
        value: king-of-red-lions
