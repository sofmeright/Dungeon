apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: compass

resources:
  - ../../../base/chrony

patches:
  - path: service-patch.yaml
    target:
      kind: Service
      name: chrony
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: chrony