apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gpu-operator
resources:
  - ../../../base/gpu-operator
  - configmap-toolkit-entrypoint-patch.yaml
patches:
  - path: helmrelease-patch.yaml
    target:
      kind: HelmRelease
      name: gpu-operator
