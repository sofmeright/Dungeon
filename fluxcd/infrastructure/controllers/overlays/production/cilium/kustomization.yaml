apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/cilium

patches:
  - path: namespace-patch.yaml
  - path: helmrelease-patch.yaml
    target:
      kind: HelmRelease
      name: cilium