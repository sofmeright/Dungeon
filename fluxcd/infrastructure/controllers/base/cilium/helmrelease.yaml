apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cilium
  namespace: flux-system
spec:
  targetNamespace: kube-system
  interval: 30m
  chart:
    spec:
      chart: cilium
      version: "1.18.2"
      sourceRef:
        kind: HelmRepository
        name: cilium
        namespace: flux-system
  values:
    operator:
      replicas: 1
    
    ipam:
      mode: kubernetes
    
    ipv4NativeRoutingCIDR: "192.168.22.0/16"
    
    k8sServiceHost: "172.22.22.105"
    k8sServicePort: 6443
    
    kubeProxyReplacement: false
    routingMode: native
    
    bgpControlPlane:
      enabled: true
      secretsNamespace:
        create: false
        name: kube-system
      
    defaultLBServiceIPAM: lbipam
    
    externalIPs:
      enabled: true
    
    hostServices:
      enabled: true
      protocols: tcp,udp
    
    nodePort:
      enabled: true
    
    hostPort:
      enabled: true
    
    bpf:
      masquerade: true
      
    hubble:
      enabled: true
      relay:
        enabled: true
      ui:
        enabled: true
        
    prometheus:
      enabled: true
      serviceMonitor:
        enabled: false