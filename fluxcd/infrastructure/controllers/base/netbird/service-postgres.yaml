apiVersion: v1
kind: Service
metadata:
  name: netbird-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    cnpg.io/cluster: netbird-postgres
