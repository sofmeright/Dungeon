apiVersion: v1
kind: Service
metadata:
  name: netbird-management
spec:
  type: ClusterIP
  ports:
  - port: 443
    targetPort: 443
    name: https
  selector:
    app: netbird
    component: management
