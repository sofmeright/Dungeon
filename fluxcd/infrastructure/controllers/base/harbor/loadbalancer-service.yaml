apiVersion: v1
kind: Service
metadata:
  name: harbor-lb
spec:
  type: LoadBalancer
  ports:
  - name: https
    port: 9483
    targetPort: 8443
    protocol: TCP
  selector:
    app: harbor
    component: nginx
