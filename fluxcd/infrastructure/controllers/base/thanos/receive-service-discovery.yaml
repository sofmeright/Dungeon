apiVersion: v1
kind: Service
metadata:
  name: thanos-receive-discovery
  labels:
    app.kubernetes.io/name: thanos-receive
    app.kubernetes.io/component: receive
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: grpc
      port: 10901
      targetPort: grpc
      protocol: TCP
    - name: http
      port: 10902
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: thanos-receive
    app.kubernetes.io/component: receive
