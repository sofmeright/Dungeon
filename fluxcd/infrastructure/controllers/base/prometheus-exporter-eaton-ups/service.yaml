apiVersion: v1
kind: Service
metadata:
  name: prometheus-exporter-eaton-ups
spec:
  type: ClusterIP
  selector:
    app: prometheus-exporter-eaton-ups
  ports:
    - port: 80
      targetPort: 9795
      protocol: TCP
      name: metrics
