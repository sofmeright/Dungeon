apiVersion: v1
kind: Service
metadata:
  name: zitadel-postgres
  namespace: prplanit-atlas
spec:
  type: ClusterIP
  selector:
    app: zitadel-postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres