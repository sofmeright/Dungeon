apiVersion: v1
kind: Service
metadata:
  name: zitadel
  namespace: zitadel
  annotations:
    io.cilium/lb-ipam-ips: "172.22.30.140"  # Pick an available IP
spec:
  type: LoadBalancer
  selector:
    app: zitadel
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http