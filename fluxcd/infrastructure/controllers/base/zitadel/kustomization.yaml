apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - postgres-cluster.yaml
  - zitadel-serviceaccount.yaml
  - zitadel-rbac.yaml
  - zitadel-clusterrole.yaml
  - zitadel-statefulset.yaml
  - zitadel-service.yaml
  - zitadel-initjob.yaml
  - zitadel-setupjob.yaml
  - admin-sa-provision-job.yaml
  - enable-loginv2-job.yaml
  - login-v2-deployment.yaml
  - login-v2-service.yaml