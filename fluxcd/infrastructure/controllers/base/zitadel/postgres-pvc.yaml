apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zitadel-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: PLACEHOLDER_STORAGE_CLASS
  resources:
    requests:
      storage: 20Gi