apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zitadel-postgres-data
  namespace: zitadel
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
  resources:
    requests:
      storage: 20Gi