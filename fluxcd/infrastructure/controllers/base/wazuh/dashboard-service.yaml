apiVersion: v1
kind: Service
metadata:
  name: wazuh-dashboard
spec:
  type: ClusterIP
  selector:
    app: wazuh-dashboard
  ports:
  - port: 80
    targetPort: 5601
    protocol: TCP
    name: http
