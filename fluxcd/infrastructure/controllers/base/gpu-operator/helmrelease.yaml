apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gpu-operator
spec:
  interval: 30m
  chart:
    spec:
      chart: gpu-operator
      version: v25.3.4
      sourceRef:
        kind: HelmRepository
        name: nvidia
      interval: 12h
  install:
    crds: Create
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  values:
    # Operator configuration
    operator:
      defaultRuntime: crio
      runtimeClass: nvidia

    # Driver configuration
    driver:
      enabled: true
      # Use pre-installed drivers if available, otherwise container will install
      usePreinstalledDrivers: false
      # Driver 570.172.08+ supports both GTX 980 Ti and A2000 GPUs
      version: "570.172.08"

    # Toolkit configuration
    toolkit:
      enabled: true

    # Device plugin configuration
    devicePlugin:
      enabled: true

    # DCGM exporter for GPU metrics
    dcgmExporter:
      enabled: true

    # GPU Feature Discovery
    gfd:
      enabled: true

    # MIG Manager (Multi-Instance GPU)
    migManager:
      enabled: false

    # Node Feature Discovery
    nfd:
      enabled: true

    # Validator
    validator:
      enabled: true
      version: v25.3.4
