apiVersion: v1
kind: Service
metadata:
  name: vault-lb
  namespace: vault
  annotations:
    io.cilium/lb-ipam-ips: "172.22.144.130"
spec:
  type: LoadBalancer
  selector:
    app: vault
  ports:
    - port: 8200
      targetPort: 8200
      protocol: TCP
      name: api