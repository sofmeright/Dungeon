apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault-data
  namespace: prplanit-atlas
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd-retain  # Use Retain policy to prevent data loss
  resources:
    requests:
      storage: 10Gi