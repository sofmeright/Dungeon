apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vault
resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - statefulset.yaml
  - service.yaml
  - vault-unseal-sidecar.yaml
patchesStrategicMerge:
  - statefulset-patch.yaml