apiVersion: v1
kind: Service
metadata:
  name: chrony
  annotations:
    lbipam.cilium.io/ips: 172.22.30.122
    lbipam.cilium.io/sharing-key: compass
    lbipam.cilium.io/sharing-cross-namespace: "*"
    gatus.home-operations.com/endpoint: |
      group: "DNS & Network (Compass🧭)"
      url: http://172.22.30.122:8080
      interval: 60s
      conditions:
        - "[STATUS] == 200"
