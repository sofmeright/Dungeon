apiVersion: ceph.rook.io/v1
kind: CephCluster
metadata:
  name: rook-ceph-external
spec:
  cephVersion:
    image: quay.io/ceph/ceph:v19
  # Crash collector disabled - no Ceph daemons run in K8s for external clusters
  crashCollector:
    disable: true
