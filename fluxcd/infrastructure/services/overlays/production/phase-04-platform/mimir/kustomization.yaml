apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gossip-stone
resources:
  - ../../../../base/mimir
  - kafka-metrics-configmap.yaml  # Mimir's Kafka metrics config
  - kafka-cluster.yaml  # Mimir's dedicated Kafka cluster
patches:
  - path: values-patch.yaml
    target:
      kind: HelmRelease
      name: mimir
