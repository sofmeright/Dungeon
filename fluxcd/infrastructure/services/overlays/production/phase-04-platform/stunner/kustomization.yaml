apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: king-of-red-lions

resources:
  - ../../../../base/stunner

patches:
  - path: helmrelease-patch.yaml
    target:
      kind: HelmRelease
      name: stunner
