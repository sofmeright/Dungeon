apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: harbor
spec:
  targetNamespace: hyrule-castle
  install:
    timeout: 15m
  valuesFrom:
    - kind: Secret
      name: harbor-secrets
      valuesKey: postgres_password
      targetPath: database.external.password
    - kind: Secret
      name: harbor-secrets
      valuesKey: s3_accesskey
      targetPath: persistence.imageChartStorage.s3.accesskey
    - kind: Secret
      name: harbor-secrets
      valuesKey: s3_secretkey
      targetPath: persistence.imageChartStorage.s3.secretkey
  values:
    # External URL
    externalURL: https://cr.pcfae.com

    # Harbor components - 3 replicas for HA with fully qualified images
    portal:
      replicas: 3
      image:
        repository: docker.io/goharbor/harbor-portal

    core:
      replicas: 3
      podLabels:
        policy.prplanit.com/cap.postgres: "true"
        policy.prplanit.com/access-scope-postgres: hyrule-castle.harbor.postgres
        policy.prplanit.com/cap.redis: "true"
        policy.prplanit.com/access-scope-redis: hyrule-castle.harbor.redis
        policy.prplanit.com/cap.s3: "true"
        policy.prplanit.com/access-scope-s3: gorons-bracelet.rgw
      image:
        repository: docker.io/goharbor/harbor-core

    jobservice:
      replicas: 3
      podLabels:
        policy.prplanit.com/cap.redis: "true"
        policy.prplanit.com/access-scope-redis: hyrule-castle.harbor.redis
        policy.prplanit.com/cap.s3: "true"
        policy.prplanit.com/access-scope-s3: gorons-bracelet.rgw
      image:
        repository: docker.io/goharbor/harbor-jobservice

    registry:
      replicas: 3
      podLabels:
        policy.prplanit.com/cap.s3: "true"
        policy.prplanit.com/access-scope-s3: gorons-bracelet.rgw
      registry:
        image:
          repository: docker.io/goharbor/registry-photon
      controller:
        image:
          repository: docker.io/goharbor/harbor-registryctl

    # Expose via ClusterIP - TLS terminated at xylem gateway
    expose:
      type: clusterIP
      tls:
        enabled: false

    nginx:
      podLabels:
        policy.prplanit.com/ingress: "true"
      image:
        repository: docker.io/goharbor/nginx-photon

    # External PostgreSQL via CloudNativePG
    database:
      type: external
      external:
        host: harbor-postgres-rw
        port: 5432
        username: harbor
        password: ""  # Injected by valuesFrom
        coreDatabase: registry
        notaryServerDatabase: notaryserver
        notarySignerDatabase: notarysigner

    # External Redis (our 3-replica StatefulSet)
    redis:
      type: external
      external:
        addr: harbor-redis-0.harbor-redis.hyrule-castle.svc.cluster.local:6379

    # Storage configuration - Ceph RGW (S3 compatible)
    persistence:
      enabled: true
      persistentVolumeClaim:
        registry:
          storageClass: ceph-rbd-retain
          size: 50Gi
        jobservice:
          jobLog:
            # Use CephFS (RWX) for multi-replica jobservice
            existingClaim: hyrule-castle-harbor-jobservice
            storageClass: ""
            size: ""
      imageChartStorage:
        type: s3
        s3:
          region: us-east-1
          bucket: harbor
          regionendpoint: https://s3.pcfae.com
    # Admin password from existing secret
    existingSecretAdminPassword: harbor-secrets
    existingSecretAdminPasswordKey: admin_password

    # Disable Trivy and Notary for now
    trivy:
      enabled: false

    notary:
      enabled: false

  # AWS SDK v1 (used by distribution S3 driver) auto-enables path-style
  # when regionendpoint is set — no forcepathstyle override needed.
  postRenderers:
    - kustomize:
        patches:
          - target:
              kind: ConfigMap
              name: harbor-registry
            patch: |
              apiVersion: v1
              kind: ConfigMap
              metadata:
                name: harbor-registry
              data:
                config.yml: |
                  version: 0.1
                  log:
                    level: info
                    fields:
                      service: registry
                  storage:
                    s3:
                      region: us-east-1
                      bucket: harbor
                      regionendpoint: https://s3.pcfae.com
                    cache:
                      layerinfo: redis
                    maintenance:
                      uploadpurging:
                        enabled: true
                        age: 168h
                        interval: 24h
                        dryrun: false
                    delete:
                      enabled: true
                    redirect:
                      disable: false
                  redis:
                    addr: harbor-redis-0.harbor-redis.hyrule-castle.svc.cluster.local:6379
                    db: 2
                    readtimeout: 10s
                    writetimeout: 10s
                    dialtimeout: 10s
                    enableTLS: false
                    pool:
                      maxidle: 100
                      maxactive: 500
                      idletimeout: 60s
                  http:
                    addr: :5000
                    relativeurls: false
                    debug:
                      addr: localhost:5001
                  auth:
                    htpasswd:
                      realm: harbor-registry-basic-realm
                      path: /etc/registry/passwd
                  validation:
                    disabled: true
                  compatibility:
                    schema1:
                      enabled: true
