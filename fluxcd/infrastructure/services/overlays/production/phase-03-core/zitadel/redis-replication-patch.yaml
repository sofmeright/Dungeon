apiVersion: redis.redis.opstreelabs.in/v1beta2
kind: RedisReplication
metadata:
  name: zitadel-redis
spec:
  storage:
    volumeClaimTemplate:
      spec:
        storageClassName: ceph-rbd
        resources:
          requests:
            storage: 64Mi
