apiVersion: v1
kind: Service
metadata:
  name: adguardhome-sync
spec:
  selector:
    app: adguardhome-sync
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
  type: ClusterIP
