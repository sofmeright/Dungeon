apiVersion: v1
kind: Service
metadata:
  name: prometheus-exporter-pve
spec:
  type: ClusterIP
  selector:
    app: prometheus-exporter-pve
  ports:
    - port: 80
      targetPort: 9221
      protocol: TCP
      name: metrics
