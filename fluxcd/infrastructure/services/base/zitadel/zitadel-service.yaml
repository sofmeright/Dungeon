apiVersion: v1
kind: Service
metadata:
  name: zitadel
spec:
  type: ClusterIP
  selector:
    app: zitadel
  ports:
    - port: 8174  # Same as Docker setup
      targetPort: 8080
      protocol: TCP
      name: http