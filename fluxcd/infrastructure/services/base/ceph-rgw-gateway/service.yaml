apiVersion: v1
kind: Service
metadata:
  name: ceph-rgw-gateway
spec:
  type: ClusterIP
  selector:
    app: ceph-rgw-gateway
  ports:
    - name: https
      port: 443
      targetPort: 8443
      protocol: TCP
