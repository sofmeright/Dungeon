apiVersion: v1
kind: Service
metadata:
  name: ceph-rgw-gateway
spec:
  type: ClusterIP
  selector:
    app: ceph-rgw-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
