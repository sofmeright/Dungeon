apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: wazuh-manager-env
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: PLACEHOLDER_SECRET_STORE
  target:
    name: wazuh-manager-env
  dataFrom:
    - extract:
        key: PLACEHOLDER_VAULT_PATH_MANAGER_ENV
