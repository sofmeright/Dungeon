apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: wazuh-indexer-env
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: PLACEHOLDER_SECRET_STORE
  target:
    name: wazuh-indexer-env
  dataFrom:
    - extract:
        key: PLACEHOLDER_VAULT_PATH_INDEXER_ENV
