apiVersion: v1
kind: Service
metadata:
  name: wazuh-dashboard
spec:
  type: ClusterIP
  selector:
    app: wazuh-dashboard
  ports:
  - port: 5601
    targetPort: 5601
    protocol: TCP
    name: https
