apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - restrict-default-sa.yaml
  - mutate-redis-sentinel-sa.yaml
  - mutate-stunner-gateway-sa.yaml
  - gate-breakglass-label.yaml
  - enforce-image-policy.yaml
