apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vault
resources:
  - test-sops.enc.yaml         # Test SOPS decryption
  # - vault-unseal-keys.enc.yaml  # SOPS-encrypted unseal keys
  # - vault-cr.yaml              # Bank-Vaults Vault Custom Resource
  # - clustersecretstore.yaml     # Uncomment after External Secrets is ready