apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vault
resources:
  - vault-unseal-keys-fixed.enc.yaml  # SOPS-encrypted unseal keys (fixed)
  # - clustersecretstore.yaml     # Uncomment after External Secrets is ready