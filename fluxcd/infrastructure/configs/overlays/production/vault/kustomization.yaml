apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vault
resources:
  # Note: Apply these after Vault is initialized
  # - vault-unseal-keys.enc.yaml  # Uncomment after encrypting with SOPS
  # - clustersecretstore.yaml     # Uncomment after External Secrets is ready