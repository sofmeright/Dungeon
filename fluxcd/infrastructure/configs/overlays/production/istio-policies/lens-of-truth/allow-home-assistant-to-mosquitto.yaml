apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-home-assistant-to-mosquitto
spec:
  selector:
    matchLabels:
      app: mosquitto
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/lens-of-truth/sa/home-assistant
      to:
        - operation:
            ports: ["1883"]
