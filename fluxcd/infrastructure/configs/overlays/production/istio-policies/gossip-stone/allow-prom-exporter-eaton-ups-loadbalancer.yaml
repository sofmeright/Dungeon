apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-prom-exporter-eaton-ups-loadbalancer
spec:
  selector:
    matchLabels:
      app: prometheus-exporter-eaton-ups
  action: ALLOW
  rules:
    - from:
        - source:
            notPrincipals:
              - cluster.local/ns/*/sa/*
      to:
        - operation:
            ports: ["9795"]
