apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-ntfy-smtp-loadbalancer
spec:
  selector:
    matchLabels:
      app: ntfy
  action: ALLOW
  rules:
    - from:
        - source:
            notPrincipals:
              - cluster.local/ns/*/sa/*
      to:
        - operation:
            ports: ["25"]
