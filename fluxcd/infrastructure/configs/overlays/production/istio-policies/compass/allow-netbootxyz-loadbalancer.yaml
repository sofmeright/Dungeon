apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-netbootxyz-loadbalancer
spec:
  selector:
    matchLabels:
      app: netbootxyz
  action: ALLOW
  rules:
    - from:
        - source:
            notPrincipals:
              - cluster.local/ns/*/sa/*
      to:
        - operation:
            ports: ["69"]
