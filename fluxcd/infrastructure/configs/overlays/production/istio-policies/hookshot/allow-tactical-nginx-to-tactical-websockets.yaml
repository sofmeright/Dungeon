apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-tactical-nginx-to-tactical-websockets
spec:
  selector:
    matchLabels:
      app: tactical-websockets
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/hookshot/sa/tactical-nginx
      to:
        - operation:
            ports: ["8383"]
