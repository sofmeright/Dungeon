apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-guacamole-to-guacamole-postgres
spec:
  selector:
    matchLabels:
      app: guacamole-postgres
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/hookshot/sa/guacamole
      to:
        - operation:
            ports: ["5432"]
