apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-teleport-loadbalancer
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: teleport
  action: ALLOW
  rules:
    - from:
        - source:
            notPrincipals:
              - cluster.local/ns/*/sa/*
      to:
        - operation:
            ports: ["443"]
