apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-zitadel-postgres-replication
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: zitadel-postgres
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/zeldas-lullaby/sa/zitadel-postgres
      to:
        - operation:
            ports: ["5432"]
