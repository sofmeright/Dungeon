apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-paperless-webserver-to-paperless-redis
spec:
  selector:
    matchLabels:
      app: paperless-redis
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/pedestal-of-time/sa/paperless-webserver
      to:
        - operation:
            ports: ["6379"]
