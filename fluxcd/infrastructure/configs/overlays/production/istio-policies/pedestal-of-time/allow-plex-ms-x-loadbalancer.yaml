apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-plex-ms-x-loadbalancer
spec:
  selector:
    matchLabels:
      app: plex-ms-x
  action: ALLOW
  rules:
    - from:
        - source:
            notPrincipals:
              - cluster.local/ns/*/sa/*
      to:
        - operation:
            ports: ["32400", "8324", "32469", "1900", "32410", "32412", "32413", "32414"]
