apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-opnform-ingress-to-opnform-api
  namespace: hyrule-castle
spec:
  selector:
    matchLabels:
      app: opnform-api
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/hyrule-castle/sa/opnform-ingress
      to:
        - operation:
            ports: ["9000"]
