apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-ceph-rgw-loadbalancer
spec:
  selector:
    matchLabels:
      app: ceph-rgw
  action: ALLOW
  rules:
    - from:
        - source:
            notPrincipals:
              - cluster.local/ns/*/sa/*
      to:
        - operation:
            ports: ["80"]
