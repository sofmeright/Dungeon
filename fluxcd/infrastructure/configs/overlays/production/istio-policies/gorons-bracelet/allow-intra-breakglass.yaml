apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-intra-breakglass
spec:
  selector:
    matchLabels:
      zt.breakglass/intra: "true"
  action: ALLOW
  rules:
    - from:
        - source:
            namespaces: ["gorons-bracelet"]
