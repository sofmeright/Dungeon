apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-homarr-postgres-replication
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: homarr-postgres
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/lost-woods/sa/homarr-postgres
      to:
        - operation:
            ports: ["5432"]
