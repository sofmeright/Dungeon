apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-reactive-resume-app-to-reactive-resume-minio
spec:
  selector:
    matchLabels:
      app: reactive-resume-minio
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/temple-of-time/sa/reactive-resume-app
      to:
        - operation:
            ports: ["9000"]
