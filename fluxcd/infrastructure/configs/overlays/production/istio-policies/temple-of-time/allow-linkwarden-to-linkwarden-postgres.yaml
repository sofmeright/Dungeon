apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-linkwarden-to-linkwarden-postgres
spec:
  selector:
    matchLabels:
      app: linkwarden-postgres
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/temple-of-time/sa/linkwarden
      to:
        - operation:
            ports: ["5432"]
