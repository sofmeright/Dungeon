apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: compass

resources:
  - ../../../base/chrony

patches:
  - path: config-patch.yaml
    target:
      kind: ConfigMap
      name: chrony-config