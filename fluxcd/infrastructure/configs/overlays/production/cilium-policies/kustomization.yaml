apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Phase 1: Cluster-wide CCNPs (additive, no default-deny)
  # 10 infrastructure + 5 contract CCNPs from base
  - ../../../base/cilium-policies
  # Phase 3: Per-namespace contract CNPs (incremental rollout)
  # Batch 1 — Minimal
  - wallmaster
  - fairy-bottle
  # king-of-red-lions deferred — istiod/stunner traffic patterns need
  # full characterization before default-deny (gateway xDS, stunner
  # operator internal, cert-manager probes)
  # Batch 2 — Simple
  - arylls-lookout
  - kokiri-forest
  - delivery-bag
  - compass
  - swift-sail
  # Batch 3 — Mixed
  - lost-woods
  - shooting-gallery
  - pedestal-of-time
  - lens-of-truth
  - tingle-tuner
  # Batch 4 — Heavy
  - gorons-bracelet
  - gossip-stone
  - hookshot
  - zeldas-lullaby
