apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prplanit-dns
resources:
  - adguard-secret.enc.yaml
  - cilium-network-policy.yaml
patchesStrategicMerge:
  - adguard-config-patch.yaml
  - deployment-patch.yaml