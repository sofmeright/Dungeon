apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: swift-sail

resources:
  - ../../../../base/smb-volumes

patches:
  - path: external-secrets-patch.yaml
  - path: persistent-volumes-patch.yaml
