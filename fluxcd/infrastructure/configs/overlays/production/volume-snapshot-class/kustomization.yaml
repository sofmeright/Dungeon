apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/volume-snapshot-class

patches:
  - path: volumesnapshotclass-patch.yaml
    target:
      kind: VolumeSnapshotClass
