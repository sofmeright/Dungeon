apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-gateway-ingress-phloem
spec:
  selector:
    matchLabels:
      policy.prplanit.com/ingress: "true"
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/kokiri-forest/sa/phloem-gateway-istio
      to:
        - operation:
            ports: ["80", "443", "3000", "5000", "8080", "8443", "9000"]
