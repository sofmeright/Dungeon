apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - zitadel-config.yaml  # Non-sensitive config only
  - admin-sa-externalsecret.yaml  # Admin SA PAT from Vault
  - login-v2-externalsecret.yaml  # Login V2 PAT from Vault