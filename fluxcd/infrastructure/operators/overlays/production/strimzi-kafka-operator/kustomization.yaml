apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/strimzi-kafka-operator
  - ../../../base/strimzi-kafka-operator/helmrepository.yaml
patches:
  - path: values-patch.yaml
    target:
      kind: HelmRelease
      name: strimzi-kafka-operator
  - target:
      kind: HelmRelease
      name: strimzi-kafka-operator
    patch: |-
      - op: replace
        path: /metadata/namespace
        value: gorons-bracelet
