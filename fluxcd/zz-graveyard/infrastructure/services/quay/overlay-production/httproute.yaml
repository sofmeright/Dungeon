apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: quay
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
  hostnames:
    - "quay.pcfae.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: quay
          port: 8080
