apiVersion: v1
kind: ConfigMap
metadata:
  name: open-webui-redis-sentinel-config
  labels:
    app: open-webui-sentinel
data:
  sentinel.conf: |
    # Redis Sentinel configuration for Open WebUI
    port 26379
    dir /data

    # Master monitoring configuration will be added by initContainer
    # Placeholder for dynamic configuration

    # Logging
    loglevel notice
    logfile ""
