apiVersion: v1
kind: Service
metadata:
  name: joplin-postgres
  labels:
    app: joplin
    component: postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: postgres
    protocol: TCP
    name: postgres
  selector:
    app: joplin
    component: postgres
