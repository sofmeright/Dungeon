apiVersion: v1
kind: Service
metadata:
  name: joplin-app
  labels:
    app: joplin
    component: app
spec:
  type: ClusterIP
  ports:
  - port: 22300
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: joplin
    component: app
