apiVersion: v1
kind: Service
metadata:
  name: netbird-signal
  labels:
    app: netbird
    component: signal
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: netbird
    component: signal
