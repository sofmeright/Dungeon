apiVersion: v1
kind: Service
metadata:
  name: netbird-postgres
  labels:
    app: netbird
    component: postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: postgres
    protocol: TCP
    name: postgres
  selector:
    app: netbird
    component: postgres
