apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: shinobi-postgres-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: PLACEHOLDER_SECRET_STORE
    kind: ClusterSecretStore
  target:
    name: shinobi-postgres-credentials
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
  - secretKey: username
    remoteRef:
      key: apps/shinobi
      property: username
  - secretKey: password
    remoteRef:
      key: apps/shinobi
      property: password
