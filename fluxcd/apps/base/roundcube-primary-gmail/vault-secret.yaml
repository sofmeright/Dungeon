apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: roundcube-primary-gmail-secrets
spec:
  secretStoreRef:
    name: PLACEHOLDER_SECRET_STORE
    kind: ClusterSecretStore
  target:
    name: roundcube-primary-gmail-secrets
  dataFrom:
    - extract:
        key: apps/roundcube-primary-gmail
