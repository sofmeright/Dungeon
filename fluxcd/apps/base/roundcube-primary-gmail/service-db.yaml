apiVersion: v1
kind: Service
metadata:
  name: roundcube-primary-gmail-db
spec:
  type: ClusterIP
  selector:
    app: roundcube-primary-gmail-db
  ports:
  - name: mysql
    port: 3306
    targetPort: 3306
    protocol: TCP
