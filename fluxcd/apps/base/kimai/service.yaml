apiVersion: v1
kind: Service
metadata:
  name: kimai
  labels:
    app: kimai
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8001
    targetPort: 8001
    protocol: TCP
  selector:
    app: kimai
