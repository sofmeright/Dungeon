apiVersion: v1
kind: Service
metadata:
  name: openwakeword
spec:
  type: ClusterIP
  selector:
    app: openwakeword
  ports:
    - name: wyoming
      port: 10400
      targetPort: wyoming
      protocol: TCP
