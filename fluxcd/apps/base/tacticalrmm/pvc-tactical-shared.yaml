apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tactical-shared
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: PLACEHOLDER_STORAGE_CLASS
  resources:
    requests:
      storage: PLACEHOLDER_STORAGE_SIZE
