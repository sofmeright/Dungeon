apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: kubernetes-hyrule-castle
spec:
  provider:
    kubernetes:
      remoteNamespace: PLACEHOLDER_REMOTE_NAMESPACE
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
      auth:
        serviceAccount:
          name: tacticalrmm-tls-reader
