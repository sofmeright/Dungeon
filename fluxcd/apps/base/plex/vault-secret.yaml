apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: plex-samba-creds
  namespace: plex
spec:
  type: kv-v2
  mount: secret
  path: plex/samba
  destination:
    name: plex-samba-creds
    create: true
  refreshAfter: 30s
  vaultAuthRef: vault