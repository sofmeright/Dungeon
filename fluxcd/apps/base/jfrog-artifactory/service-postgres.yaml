apiVersion: v1
kind: Service
metadata:
  name: jfrog-artifactory-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5433
    targetPort: 5433
    name: postgres
  selector:
    app: jfrog-artifactory-postgres
