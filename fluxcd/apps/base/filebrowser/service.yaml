apiVersion: v1
kind: Service
metadata:
  name: filebrowser
spec:
  type: PLACEHOLDER_SERVICE_TYPE
  selector:
    app: filebrowser
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
