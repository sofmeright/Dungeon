apiVersion: v1
kind: Service
metadata:
  name: vlmcsd
spec:
  type: LoadBalancer
  selector:
    app: vlmcsd
  ports:
  - name: kms
    port: PLACEHOLDER_KMS_PORT
    targetPort: kms
    protocol: TCP
