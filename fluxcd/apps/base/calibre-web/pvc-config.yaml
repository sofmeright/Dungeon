apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-web-config-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: PLACEHOLDER_STORAGE_CLASS