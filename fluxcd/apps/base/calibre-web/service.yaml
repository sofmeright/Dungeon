apiVersion: v1
kind: Service
metadata:
  name: calibre-web
  labels:
    app: calibre-web
spec:
  type: ClusterIP
  externalTrafficPolicy: Cluster
  selector:
    app: calibre-web
  ports:
    - name: web
      port: 8083
      targetPort: 8083
      protocol: TCP
