apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - vault-secret.yaml
  - smb-volumes.yaml
  - deployment.yaml
  - service.yaml
  # pvc-config.yaml removed - production uses static RBD provisioning