apiVersion: v1
kind: Service
metadata:
  name: openspeedtest
spec:
  type: ClusterIP
  selector:
    app: openspeedtest
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: https
