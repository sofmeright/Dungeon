apiVersion: v1
kind: Service
metadata:
  name: faster-whisper
spec:
  type: ClusterIP
  selector:
    app: faster-whisper
  ports:
    - port: 10300
      targetPort: http
      protocol: TCP
      name: http
