apiVersion: v1
kind: Service
metadata:
  name: faster-whisper
spec:
  type: LoadBalancer
  selector:
    app: faster-whisper
  ports:
  - port: 10300
    targetPort: http
    protocol: TCP
    name: http
