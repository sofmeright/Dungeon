apiVersion: v1
kind: Service
metadata:
  name: speedtest-tracker-postgres
spec:
  type: ClusterIP
  selector:
    app: speedtest-tracker-postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
