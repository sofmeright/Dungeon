apiVersion: v1
kind: Service
metadata:
  name: librespeed-speedtest
spec:
  type: ClusterIP
  selector:
    app: librespeed-speedtest
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
