apiVersion: v1
kind: Service
metadata:
  name: google-webfonts-helper
spec:
  type: LoadBalancer
  selector:
    app: google-webfonts-helper
  ports:
  - name: http
    port: PLACEHOLDER_HTTP_PORT
    targetPort: http
    protocol: TCP
