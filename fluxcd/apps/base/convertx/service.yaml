apiVersion: v1
kind: Service
metadata:
  name: convertx
spec:
  type: PLACEHOLDER_SERVICE_TYPE
  selector:
    app: convertx
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
