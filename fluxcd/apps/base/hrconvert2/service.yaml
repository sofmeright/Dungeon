apiVersion: v1
kind: Service
metadata:
  name: hrconvert2
spec:
  type: LoadBalancer
  selector:
    app: hrconvert2
  ports:
  - name: http
    port: PLACEHOLDER_HTTP_PORT
    targetPort: http
    protocol: TCP
