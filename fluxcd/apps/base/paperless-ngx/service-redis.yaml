apiVersion: v1
kind: Service
metadata:
  name: paperless-redis
spec:
  type: ClusterIP
  selector:
    app: paperless-redis
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
