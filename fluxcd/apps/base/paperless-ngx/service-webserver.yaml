apiVersion: v1
kind: Service
metadata:
  name: paperless-webserver
spec:
  type: ClusterIP
  selector:
    app: paperless-webserver
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
