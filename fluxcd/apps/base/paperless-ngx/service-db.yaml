apiVersion: v1
kind: Service
metadata:
  name: paperless-db
spec:
  type: ClusterIP
  selector:
    app: paperless-db
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
