apiVersion: v1
kind: Service
metadata:
  name: calcom-db
spec:
  selector:
    cnpg.io/cluster: calcom-postgres
    role: primary
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  type: ClusterIP
