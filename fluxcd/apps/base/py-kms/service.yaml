apiVersion: v1
kind: Service
metadata:
  name: py-kms
spec:
  type: PLACEHOLDER_SERVICE_TYPE
  selector:
    app: py-kms
  ports:
  - name: kms
    port: 1688
    targetPort: 1688
    protocol: TCP
  - name: web
    port: 8080
    targetPort: 8080
    protocol: TCP
