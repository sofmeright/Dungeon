apiVersion: v1
kind: Service
metadata:
  name: nextcloud-collabora
  labels:
    app: nextcloud-collabora
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9980
      targetPort: http
      protocol: TCP
  selector:
    app: nextcloud-collabora
