apiVersion: v1
kind: Service
metadata:
  name: nextcloud-imaginary
  labels:
    app: nextcloud-imaginary
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9000
      targetPort: http
      protocol: TCP
  selector:
    app: nextcloud-imaginary
