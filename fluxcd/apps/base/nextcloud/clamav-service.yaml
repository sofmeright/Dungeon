apiVersion: v1
kind: Service
metadata:
  name: nextcloud-clamav
  labels:
    app: nextcloud-clamav
spec:
  type: ClusterIP
  ports:
    - name: clamd
      port: 3310
      targetPort: clamd
      protocol: TCP
  selector:
    app: nextcloud-clamav
