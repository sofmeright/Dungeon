apiVersion: v1
kind: Service
metadata:
  name: nextcloud-whiteboard
  labels:
    app: nextcloud-whiteboard
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3002
      targetPort: http
      protocol: TCP
  selector:
    app: nextcloud-whiteboard
