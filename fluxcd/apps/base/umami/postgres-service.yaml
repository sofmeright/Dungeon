apiVersion: v1
kind: Service
metadata:
  name: umami-postgres
  labels:
    app.kubernetes.io/name: umami-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: postgres
    protocol: TCP
    name: postgres
  selector:
    app.kubernetes.io/name: umami-postgres
