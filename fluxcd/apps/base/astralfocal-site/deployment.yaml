apiVersion: apps/v1
kind: Deployment
metadata:
  name: astralfocal-site
  labels:
    app: astralfocal-site
spec:
  replicas: 1
  selector:
    matchLabels:
      app: astralfocal-site
  template:
    metadata:
      labels:
        app: astralfocal-site
    spec:
      serviceAccountName: astralfocal-site
      containers:
        - name: astralfocal-site
          image: PLACEHOLDER_IMAGE
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              memory: "64Mi"
              cpu: "10m"
            limits:
              memory: "128Mi"
              cpu: "100m"
