apiVersion: v1
kind: Service
metadata:
  name: kasm
spec:
  type: LoadBalancer
  selector:
    app: kasm
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
    - port: 3443
      targetPort: https
      protocol: TCP
      name: https
