apiVersion: v1
kind: ConfigMap
metadata:
  name: mosquitto-config
data:
  mosquitto.conf: |
    # Mosquitto Base Configuration
    # Patched by overlay with production settings
    listener 1883
    persistence true
    persistence_location /mosquitto/data/
    password_file /mosquitto/data/passwd
    allow_anonymous false
