apiVersion: v1
kind: Service
metadata:
  name: appflowy-admin-frontend
  labels:
    app: appflowy-admin-frontend
    component: admin-ui
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
  selector:
    app: appflowy-admin-frontend
    component: admin-ui
