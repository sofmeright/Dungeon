apiVersion: v1
kind: Service
metadata:
  name: appflowy-cloud
  labels:
    app: appflowy-cloud
    component: backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: http
    protocol: TCP
  selector:
    app: appflowy-cloud
    component: backend
