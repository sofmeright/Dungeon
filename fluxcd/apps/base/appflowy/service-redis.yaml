apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: appflowy
    component: redis
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: appflowy
    component: redis
  ports:
  - name: redis
    port: 6379
    targetPort: redis
    protocol: TCP
