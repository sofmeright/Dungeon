apiVersion: v1
kind: Service
metadata:
  name: appflowy-redis
  labels:
    app: appflowy-redis
    component: redis
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: appflowy-redis
  ports:
  - name: redis
    port: 6379
    targetPort: redis
    protocol: TCP
