apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: appflowy
    component: postgres
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: appflowy
    component: postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: postgres
    protocol: TCP
