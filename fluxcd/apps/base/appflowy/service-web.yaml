apiVersion: v1
kind: Service
metadata:
  name: appflowy-web
  labels:
    app: appflowy-web
    component: frontend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
  selector:
    app: appflowy-web
    component: frontend
