apiVersion: v1
kind: Service
metadata:
  name: appflowy-ai
  labels:
    app: appflowy-ai
    component: ai-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 5001
    targetPort: http
    protocol: TCP
  selector:
    app: appflowy-ai
    component: ai-service
