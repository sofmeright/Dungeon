apiVersion: v1
kind: Service
metadata:
  name: appflowy-gotrue
  labels:
    app: appflowy
    component: gotrue
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 9999
    targetPort: http
    protocol: TCP
  selector:
    app: appflowy
    component: gotrue
