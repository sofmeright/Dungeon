apiVersion: v1
kind: Service
metadata:
  name: vaultwarden
spec:
  type: PLACEHOLDER_SERVICE_TYPE
  selector:
    app: vaultwarden
  ports:
  - name: http
    port: PLACEHOLDER_HTTP_PORT
    targetPort: 80
  - name: websocket
    port: PLACEHOLDER_WEBSOCKET_PORT
    targetPort: 3012
