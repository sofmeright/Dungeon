apiVersion: apps/v1
kind: Deployment
metadata:
  name: precisionplanit-site
  labels:
    app: precisionplanit-site
spec:
  replicas: 1
  selector:
    matchLabels:
      app: precisionplanit-site
  template:
    metadata:
      labels:
        app: precisionplanit-site
    spec:
      serviceAccountName: precisionplanit-site
      containers:
        - name: precisionplanit-site
          image: PLACEHOLDER_IMAGE
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              memory: "64Mi"
              cpu: "10m"
            limits:
              memory: "128Mi"
              cpu: "100m"
