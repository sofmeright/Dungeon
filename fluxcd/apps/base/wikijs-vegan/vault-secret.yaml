apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: wikijs-vegan-secrets
spec:
  secretStoreRef:
    name: PLACEHOLDER_SECRET_STORE
    kind: ClusterSecretStore
  target:
    name: wikijs-vegan-secrets
  dataFrom:
    - extract:
        key: apps/wikijs-vegan
