apiVersion: v1
kind: Service
metadata:
  name: wikijs-vegan
spec:
  type: PLACEHOLDER_SERVICE_TYPE
  selector:
    app: wikijs-vegan
  ports:
  - name: http
    port: PLACEHOLDER_PORT
    targetPort: 3000
    protocol: TCP
