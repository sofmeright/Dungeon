apiVersion: v1
kind: Service
metadata:
  name: mazanoke
  labels:
    app: mazanoke
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3474
    targetPort: http
    protocol: TCP
  selector:
    app: mazanoke
