apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: unifi-app
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: unifi-app
          image: docker.io/linuxserver/unifi-network-application:9.0.114
          env:
            - name: TZ
              value: "America/Los_Angeles"
  volumeClaimTemplates:
    - metadata:
        name: zeldas-lullaby-unifi-config
      spec:
        storageClassName: ceph-rbd-retain
