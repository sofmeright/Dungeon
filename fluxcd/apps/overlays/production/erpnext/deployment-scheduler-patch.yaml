apiVersion: apps/v1
kind: Deployment
metadata:
  name: erpnext-scheduler
spec:
  template:
    spec:
      containers:
        - name: scheduler
          image: docker.io/frappe/erpnext:v15.54.5
          resources:
            requests:
              cpu: "10m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      volumes:
        - name: sites-data
          persistentVolumeClaim:
            claimName: hyrule-castle-erpnext-sites-data-erpnext-sites-0
        - name: logs-data
          persistentVolumeClaim:
            claimName: hyrule-castle-erpnext-logs-data-erpnext-sites-0
