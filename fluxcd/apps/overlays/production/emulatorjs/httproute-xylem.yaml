apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: emulatorjs-xylem
  annotations:
    gatus.home-operations.com/endpoint: |
      group: "Game Servers (Shooting Gallery🎯)"
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
  hostnames:
    - "emulatorjs.pcfae.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: emulatorjs
          port: 80
