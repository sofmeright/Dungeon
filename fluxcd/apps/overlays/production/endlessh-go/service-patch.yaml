apiVersion: v1
kind: Service
metadata:
  name: endlessh-go
  annotations:
    lbipam.cilium.io/ips: "172.22.30.107"
    lbipam.cilium.io/sharing-key: "tingle-tuner"
    lbipam.cilium.io/sharing-cross-namespace: "*"
    gatus.home-operations.com/endpoint: |
      group: Infrastructure
      url: tcp://172.22.30.107:2222
      interval: 60s
      conditions:
        - "[CONNECTED] == true"
spec:
  type: LoadBalancer
