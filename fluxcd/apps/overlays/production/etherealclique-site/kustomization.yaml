apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lost-woods

resources:
  - ../../../base/etherealclique-site
  - httproute.yaml

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: etherealclique-site
  - path: service-patch.yaml
    target:
      kind: Service
      name: etherealclique-site
  - path: service-etherealclique-site-ingress-patch.yaml
    target:
      kind: Service
      name: etherealclique-site
