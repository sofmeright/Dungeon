apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lost-woods

resources:
  - ../../../base/precisionplanit-site

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: precisionplanit-site
