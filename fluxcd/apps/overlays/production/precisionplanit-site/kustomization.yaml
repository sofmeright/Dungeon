apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lost-woods

resources:
  - ../../../base/precisionplanit-site
  - httproute.yaml

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: precisionplanit-site
  - path: service-patch.yaml
    target:
      kind: Service
      name: precisionplanit-site
  - path: service-precisionplanit-site-ingress-patch.yaml
    target:
      kind: Service
      name: precisionplanit-site
