apiVersion: v1
kind: Service
metadata:
  name: echo-ip
  annotations:
    lbipam.cilium.io/ips: "172.22.30.122"
    lbipam.cilium.io/sharing-key: "compass"
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
