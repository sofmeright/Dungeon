apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: umami-app-secrets
spec:
  secretStoreRef:
    name: operationtimecapsule
    kind: ClusterSecretStore
  dataFrom:
    - extract:
        key: apps/umami
