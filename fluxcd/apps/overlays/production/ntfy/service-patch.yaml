apiVersion: v1
kind: Service
metadata:
  name: ntfy
  annotations:
    lbipam.cilium.io/ips: "172.22.30.71"
    lbipam.cilium.io/sharing-key: "hyrule-castle"
spec:
  type: LoadBalancer
  ports:
    - port: 1638
      targetPort: http
      protocol: TCP
      name: http
    - port: 25
      targetPort: smtp
      protocol: TCP
      name: smtp
