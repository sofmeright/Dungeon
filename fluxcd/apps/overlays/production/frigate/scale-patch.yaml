# Frigate enabled - primary NVR (replaced Shinobi)
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: frigate
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: frigate
          image: ghcr.io/blakeblackshear/frigate:0.16.3-tensorrt
