# Use standard stable image with ONNX/CUDA support instead of deprecated TensorRT
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: frigate
spec:
  template:
    spec:
      containers:
        - name: frigate
          image: ghcr.io/blakeblackshear/frigate:stable
