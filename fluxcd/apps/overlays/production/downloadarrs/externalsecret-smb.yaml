apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: downloadarrs-smb-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-great-sea
  target:
    name: downloadarrs-smb-secret
  dataFrom:
    - extract:
        key: smb/qbittorrent
