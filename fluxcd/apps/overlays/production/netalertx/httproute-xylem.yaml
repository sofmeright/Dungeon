apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: netalertx-xylem
  annotations:
    gatus.home-operations.com/endpoint: |
      group: "Monitoring (Gossip Stone💬)"
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
  hostnames:
    - "hosts.pcfae.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: netalertx
          port: 20211
