apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: truenas-webui
spec:
  host: truenas-webui.gossip-stone.svc.cluster.local
  trafficPolicy:
    portLevelSettings:
      - port:
          number: 443
        tls:
          mode: SIMPLE
          insecureSkipVerify: true
