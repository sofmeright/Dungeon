apiVersion: v1
kind: Service
metadata:
  name: uptime-kuma
  annotations:
    lbipam.cilium.io/ips: "172.22.30.137"
    lbipam.cilium.io/sharing-key: "gossip-stone"
spec:
  type: LoadBalancer
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
