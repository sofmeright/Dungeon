apiVersion: v1
kind: Service
metadata:
  name: wikijs-vegan
  annotations:
    lbipam.cilium.io/ips: "172.22.30.124"
    lbipam.cilium.io/sharing-key: "pedestal-of-time"
    lbipam.cilium.io/sharing-cross-namespace: "*"
    service.cilium.io/loadbalancer-mode: "dsr"
spec:
  type: LoadBalancer
  ports:
  - $patch: replace
  - name: http
    port: 3364
    targetPort: 3000
    protocol: TCP
