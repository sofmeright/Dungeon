apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: photoprism-x-secret
spec:
  secretStoreRef:
    name: vault-pedestal-of-time
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: photoprism-x-smb-secret
spec:
  secretStoreRef:
    name: vault-pedestal-of-time
