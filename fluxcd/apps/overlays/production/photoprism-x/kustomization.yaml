apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pedestal-of-time

resources:
  - ../../../base/photoprism-x
  - httproute-xylem.yaml

patches:
  - path: statefulset-patch.yaml
    target:
      kind: StatefulSet
      name: photoprism-x
  - path: externalsecret-patch.yaml
  - path: pv-smb-patch.yaml
  - path: storageclass-patch.yaml
