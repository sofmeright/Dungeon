apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: photoprism
spec:
  volumeClaimTemplates:
  - metadata:
      name: storage
    spec:
      storageClassName: ceph-rbd-retain
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: photoprism-mariadb
spec:
  volumeClaimTemplates:
  - metadata:
      name: database
    spec:
      storageClassName: ceph-rbd-retain
