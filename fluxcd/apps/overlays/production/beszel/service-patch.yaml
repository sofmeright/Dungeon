apiVersion: v1
kind: Service
metadata:
  name: beszel
  annotations:
    lbipam.cilium.io/ips: "172.22.30.137"
    lbipam.cilium.io/sharing-key: "gossip-stone"
spec:
  type: LoadBalancer
  ports:
    - port: 8353
      targetPort: 8090
      protocol: TCP
      name: http
