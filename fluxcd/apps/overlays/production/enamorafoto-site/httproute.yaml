apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: enamorafoto-site
  annotations:
    gatus.home-operations.com/endpoint: |
      group: "Discovery & Dashboards (Lost Woods🌲)"
spec:
  parentRefs:
    - name: cell-membrane-gateway
      namespace: hyrule-castle
  hostnames:
    - "enamorafoto.operationtimecapsule.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: enamorafoto-site
          port: 4016
