apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lost-woods

resources:
  - ../../../base/enamorafoto-site
  - httproute.yaml

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: enamorafoto-site
  - path: service-patch.yaml
    target:
      kind: Service
      name: enamorafoto-site
