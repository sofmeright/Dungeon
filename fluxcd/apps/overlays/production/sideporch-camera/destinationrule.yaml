apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: sideporch-camera
spec:
  host: sideporch-camera.lens-of-truth.svc.cluster.local
  trafficPolicy:
    portLevelSettings:
      - port:
          number: 443
        tls:
          mode: SIMPLE
          insecureSkipVerify: true
