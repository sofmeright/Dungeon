apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: organizr
  namespace: lost-woods
  annotations:
    gatus.home-operations.com/endpoint: |
      group: "Discovery & Dashboards (Lost Woods🌲)"
spec:
  parentRefs:
    - name: cell-membrane-gateway
      namespace: hyrule-castle
  hostnames:
    - "apps.prplanit.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: organizr
          port: 80
