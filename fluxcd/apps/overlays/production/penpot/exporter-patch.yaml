apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: penpot-exporter
spec:
  template:
    spec:
      imagePullSecrets:
      - name: docker-io-pull-secret
      containers:
      - name: exporter
        image: docker.jcr.pcfae.com/penpotapp/exporter:latest
