apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: home-assistant
spec:
  volumeClaimTemplates:
    - metadata:
        name: config
      spec:
        storageClassName: ceph-rbd-retain
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
