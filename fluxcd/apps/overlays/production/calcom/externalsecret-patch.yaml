apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: calcom-secrets
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-precisionplanit
  dataFrom:
  - extract:
      key: apps/calcom
