apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: song-of-time

resources:
  - ../../../base/mealie

patches:
  - path: service-patch.yaml
    target:
      kind: Service
      name: mealie
