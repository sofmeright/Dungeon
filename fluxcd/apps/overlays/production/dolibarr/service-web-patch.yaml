apiVersion: v1
kind: Service
metadata:
  name: dolibarr-web
  annotations:
    lbipam.cilium.io/ips: "172.22.30.71"
    lbipam.cilium.io/sharing-key: "hyrule-castle"
spec:
  type: LoadBalancer
  ports:
  - port: 40118
    targetPort: 80
    name: http
