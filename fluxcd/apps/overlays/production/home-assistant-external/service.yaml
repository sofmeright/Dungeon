apiVersion: v1
kind: Service
metadata:
  name: home-assistant-external
  namespace: tingle-tuner
spec:
  type: ExternalName
  externalName: home-assistant-external.tingle-tuner.svc.cluster.local
  ports:
    - port: 8123
      targetPort: 8123
      protocol: TCP
      name: http
