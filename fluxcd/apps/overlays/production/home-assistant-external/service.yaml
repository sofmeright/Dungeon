# Service for external Home Assistant
# The actual routing is handled by ServiceEntry
apiVersion: v1
kind: Service
metadata:
  name: home-assistant-external
  namespace: tingle-tuner
spec:
  ports:
    - port: 8123
      targetPort: 8123
      protocol: TCP
      name: http
