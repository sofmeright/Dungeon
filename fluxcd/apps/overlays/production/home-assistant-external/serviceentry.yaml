apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: home-assistant-external
  namespace: tingle-tuner
spec:
  hosts:
    - home-assistant-external.tingle-tuner.svc.cluster.local
  location: MESH_EXTERNAL
  ports:
    - number: 8123
      name: http
      protocol: HTTP
  resolution: STATIC
  endpoints:
    - address: 10.55.6.149
      ports:
        http: 8123
