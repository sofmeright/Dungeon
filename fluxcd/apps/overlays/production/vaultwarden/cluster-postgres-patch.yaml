apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: vaultwarden-postgres
spec:
  imageName: docker.jcr.pcfae.com/cloudnative-pg/postgresql:16

  storage:
    storageClass: ceph-block
    size: 10Gi
