apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: zeldas-lullaby

resources:
  - ../../../base/oauth2-proxy
  - httproute.yaml

patches:
  - path: deployment-oauth2-proxy-patch.yaml
  - path: service-patch.yaml
    target:
      kind: Service
      name: oauth2-proxy
  - path: externalsecret-patch.yaml
