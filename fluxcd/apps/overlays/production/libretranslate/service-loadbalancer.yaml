apiVersion: v1
kind: Service
metadata:
  name: libretranslate-lb
  annotations:
    lbipam.cilium.io/ips: "172.22.30.107"
    lbipam.cilium.io/sharing-key: "tingle-tuner"
spec:
  type: LoadBalancer
  selector:
    app: libretranslate
  ports:
  - name: http
    port: 5005
    targetPort: 5000
    protocol: TCP
