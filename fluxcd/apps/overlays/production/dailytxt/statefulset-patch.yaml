apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: dailytxt
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: dailytxt
        image: docker.jcr.pcfae.com/phitux/dailytxt:latest
        volumeMounts:
        - name: pedestal-of-time-dailytxt-data
          mountPath: /app/data
  volumeClaimTemplates:
  - metadata:
      name: pedestal-of-time-dailytxt-data
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: ceph-rbd-retain
      resources:
        requests:
          storage: 1Gi
