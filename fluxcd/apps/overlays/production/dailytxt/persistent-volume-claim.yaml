apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kai-dailytxt-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd-static
  volumeName: kai-dailytxt-data
  resources:
    requests:
      storage: 1Gi
