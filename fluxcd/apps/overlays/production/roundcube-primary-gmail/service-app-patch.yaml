apiVersion: v1
kind: Service
metadata:
  name: roundcube-primary-gmail
spec:
  type: ClusterIP
  ports:
    - $patch: replace
    - name: http
      port: 9001
      targetPort: 8080
      protocol: TCP
