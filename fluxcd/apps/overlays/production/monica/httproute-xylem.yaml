apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: monica-xylem
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
  hostnames:
    - "monica.pcfae.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: monica
          port: 80
