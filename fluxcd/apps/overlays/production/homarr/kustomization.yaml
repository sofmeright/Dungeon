apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lost-woods

resources:
  - ../../../base/homarr
  - externalsecret.yaml

patches:
  - path: helmrelease-patch.yaml
    target:
      kind: HelmRelease
      name: homarr
