apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filebrowser-srv
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 100Gi
