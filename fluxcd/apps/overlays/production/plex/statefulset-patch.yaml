apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: plex
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: plex
        image: docker.io/plexinc/pms-docker:latest
        env:
        - name: ADVERTISE_IP
          value: "http://172.22.30.123:32400/,https://plex.song-of-time.com/"
  volumeClaimTemplates:
  - metadata:
      name: config
    spec:
      storageClassName: ceph-rbd-retain
  - metadata:
      name: transcode
    spec:
      storageClassName: ceph-rbd-retain