apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: appflowy-postgres-app
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-operationtimecapsule
  data:
  # CNPG keys - required for database bootstrap
  - secretKey: username
    remoteRef:
      key: apps/appflowy
      property: POSTGRES_USER
  - secretKey: password
    remoteRef:
      key: apps/appflowy
      property: POSTGRES_PASSWORD
  # MinIO credentials - map S3 keys to MINIO keys
  - secretKey: MINIO_ROOT_USER
    remoteRef:
      key: apps/appflowy
      property: APPFLOWY_S3_ACCESS_KEY
  - secretKey: MINIO_ROOT_PASSWORD
    remoteRef:
      key: apps/appflowy
      property: APPFLOWY_S3_SECRET_KEY
  # Worker database name - use POSTGRES_DB
  - secretKey: APPFLOWY_WORKER_DATABASE_NAME
    remoteRef:
      key: apps/appflowy
      property: POSTGRES_DB
  dataFrom:
    - extract:
        key: apps/appflowy
