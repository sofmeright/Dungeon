apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: appflowy-postgres-init-sql
spec:
  secretStoreRef:
    name: vault-backend
  dataFrom:
  - extract:
      key: apps/appflowy
