apiVersion: v1
kind: ConfigMap
metadata:
  name: appflowy-app-config
data:
  # Production-specific external URLs
  APPFLOWY_WS_BASE_URL: "wss://appflowy.precisionplanit.com/ws/v2"
  API_EXTERNAL_URL: "https://appflowy.precisionplanit.com"

  # Production SMTP host (if different from base defaults)
  APPFLOWY_MAILER_SMTP_HOST: "smtp.gmail.com"
