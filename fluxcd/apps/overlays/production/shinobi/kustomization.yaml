apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: lens-of-truth
resources:
  - ../../../base/shinobi
  # - httproute.yaml  # Disabled - Frigate taking over nvr.pcfae.com
  - externalsecret-registry.yaml
patches:
  - path: statefulset-patch.yaml
    target:
      kind: StatefulSet
      name: shinobi
  - path: scale-patch.yaml
    target:
      kind: StatefulSet
      name: shinobi
