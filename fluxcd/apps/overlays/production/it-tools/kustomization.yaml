apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/it-tools

patchesStrategicMerge:
  - service-patch.yaml
  - cilium-lb-patch.yaml
  - deployment-patch.yaml

patches:
  - path: namespace-patch.yaml