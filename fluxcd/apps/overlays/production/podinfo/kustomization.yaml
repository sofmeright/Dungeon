apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tingle-tuner

resources:
  - ../../../base/podinfo

patches:
  - path: release.yaml
    target:
      kind: HelmRelease
      name: podinfo