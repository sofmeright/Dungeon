apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sofmeright-site
spec:
  parentRefs:
    - name: phloem-gateway
      namespace: kokiri-forest
  hostnames:
    - "sofmeright.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: sofmeright-site
          port: 4020
