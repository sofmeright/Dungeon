apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: lenpaste-xylem
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
  hostnames:
    - "lenpaste.pcfae.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: lenpaste-app
          port: 80
