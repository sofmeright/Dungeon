apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich
spec:
  parentRefs:
  - name: xylem-gateway
    namespace: arylls-lookout
  hostnames:
  - images.pcfae.com
  rules:
  - backendRefs:
    - name: immich
      port: 2283
