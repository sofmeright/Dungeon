apiVersion: v1
kind: Service
metadata:
  name: immich-lb
  annotations:
    lbipam.cilium.io/ips: "172.22.30.222"
    lbipam.cilium.io/sharing-key: "song-of-time"
spec:
  type: LoadBalancer
  selector:
    app: immich
  ports:
  - name: http
    port: 2283
    targetPort: 2283
    protocol: TCP
