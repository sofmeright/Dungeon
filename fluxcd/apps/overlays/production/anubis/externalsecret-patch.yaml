apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: anubis-secrets
spec:
  secretStoreRef:
    name: vault-zeldas-letter
    kind: ClusterSecretStore
  data:
  - secretKey: ed25519_key
    remoteRef:
      key: apps/anubis
      property: ed25519_key
