apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: anubis-xylem
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
  hostnames:
    - "anubis.pcfae.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: anubis
          port: 8080
