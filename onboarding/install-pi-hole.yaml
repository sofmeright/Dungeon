---

- name: Playbook to install docker compose.
  hosts: linux
  become: true
  tasks: 

    - name: deploy Docker Compose stack
      community.docker.docker_compose_v2:
        project_src: /apps/pi-hole
        files:
        - docker-compose.yaml